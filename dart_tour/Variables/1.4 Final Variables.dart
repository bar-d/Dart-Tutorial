// Swift의 let

void main() {
  // final name = "bard";
  final String name = "bard"; // 타입 선언 가능
  // name = "not bard"; 이후 할당 불가능
  print(name);
  
  final bard;
  bard = "i'm bard";
  print(bard);
}